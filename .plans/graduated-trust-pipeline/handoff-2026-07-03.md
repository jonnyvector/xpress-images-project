# Session Handoff — 2026-07-03 (graduated-trust pipeline build day)

> For the next session: read this + `MEMORY.md` before doing anything.
> The plan is COMPLETE; this doc is the operational state + the ONE open problem.
> **Operator directive: DO NOT give up on the open problem. Jonathan has
> successfully generated thousands of images including full door sets — the
> capability exists; find the technique.**

## What was built today (all committed + pushed, master)

The **graduated-trust pipeline** — full lifecycle in one day: RFC → 4-agent
review → 6 phases → COMPLETE (101/101 features, plan db `.plans/graduated-trust-pipeline/plan.db`).

- **Stable image identity** (D-006): every image has a uuid4 `image_id`;
  approvals/verdicts/ledger key off it. Old projects migrated non-destructively.
- **Server-enforced gates**: GT-001 replica approval (Stage A), GT-002 ≤5 batch
  while bulk locked (Stage B), style-class bulk unlock (Stage C).
  Knobs: `backend/qa/trust_config.json` — currently `frame_standard` UNLOCKED.
- **Async QA lane**: every stored image judged (~5s median): vision judge +
  geometry (variant-vs-replica) + policy. Auto-regen: `regenerate` verdicts
  retry ≤2 under $10/run unconsented cap.
- **Review tab** (app UI): everything awaiting operator verdict, auto-polling,
  anchors pinned. THE way Jonathan reviews. Never build ad-hoc review artifacts.
- **Reliability ledger**: `output/.qa/reliability.jsonl`. Current:
  ~55 decisive, ~93% agreement, 3 disagreements (all profile-class).
- **Coverage overrides**: `docs/sales/data/coverage_overrides.json`.
- **`/learn` accepts `aspect_ratio` param** (default 9:16 — the standard; only
  override for genuinely non-9:16 samples).

## Production state

- **Thermofoil doors coverage 10/27 marked**; five top-sellers now have
  approved anchors + reviewed sets:
  - JR7 (f8eed0f7) 5 colors ✓ · AR766 (52e076a8) 5 ✓ · DR31 (9ee943b4) 4/5
    (Alabaster rejected: recessed, should be raised) · FC712 (9bf97152) 3/5
    (Chocolate Pear + Cherry Blossom rejected: profile)
  - **DR133 (e6beca5b): 42/45 approved — full palette minus 3 whites (OPEN PROBLEM)**
- Wood doors: adobe (2564359f) + frontier (a9f04868) piloted; adobe has 4
  rejected slots parked; shaker/revere/alpine/heritage/liberty etc. all
  REPLICA READY for zero-cost approve→batch rounds.
- Standing rule (memory): **generate ALL app swatches per door, always** —
  never scope to supplier availability.
- App swatch catalog issues: `feather-white-suedette`/`suedetter` duplicate
  (one lacks a file); Concrete Groovz / Summer Flame / Wenge Deep Grain have
  no swatch images at all.
- Local Decore catalog: `~/Desktop/Xpress/Decore Catalog/` (per-code folders,
  `hero/door.jpg`, `product.json` with materials). CHECK HERE before scraping.

## RESOLVED: DR133 white-on-white (was 0/7) — SOLVED 2026-07-03

**Fix:** attach the approved replica as a reference image on the normal
signature path (`generate_variation`, temp 0.3) — it carries geometry
independent of color contrast. 5/5 correct across White Supermatte (×2), 949W,
Feather; Velvet White (a 4th rejected white, undercounted below) also fixed.
All four regenerated, approved, written into DR133 → 44/45 (only Victorian
White, a judge pass, awaits operator click). **Geometry PROSE backfires** —
describing the relief in text re-triggers the raised-panel prior; let the image
carry it. Wired into `backend/worker.py::_needs_geometry_anchor` (near-white RTF
auto-anchors, hex or swatch-luminance). Code not yet committed. Full writeup:
memory `white-on-white-reference-anchor`.

### Original problem notes (kept for context)

Three whites rejected on DR133 (White Supermatte, 949W White, Feather White
Suedetter; Victorian White eventually PASSED — note: so white CAN work).
Failure mode: the white Victoria tiered profile degrades/simplifies; judge
`profile_character` and operator agree.

**What was tried (don't repeat blindly):**
1. 6× app-path generation (signature + swatch attached + prompt) — all rejected.
2. 1× `generate_variation_from_reference` with replica image as visual
   reference — profile came out simplified single-step (operator: wrong).
   **CAVEAT: the swatch file did NOT attach in this test** (filename case
   mismatch: file is `swatches/rtf/White-Supermatte.jpg`) — the
   reference+swatch combination has NEVER actually been tested.

**Untried / next moves (in order):**
1. **Ask Jonathan how he generated white sets successfully before** — he has
   done this at scale; capture his technique first, then automate it.
2. Reference image + correctly-attached swatch file (the flawed test, done right).
3. Victorian White PASSED on retry — diff its generation params/verdict against
   the failures; what was different?
4. Multiple samples per approach, judge-scored, before declaring anything.
5. Note: one-shot experiments prove nothing here — biased-distribution regime;
   run ≥3 samples per condition.

## Tuning backlog (plan db findings, test cases attached)

TUNING-1 knot false-flags · 2 all-3s judge miss · 3 replica color leak rubric ·
4 anchor strategy (RESOLVED: native only, D-015) · 5 RTF prompt flattens subtle
grain (fix: learn under wood prompt) · 6 RTF grain-direction rubric rule
(vertical doors / horizontal drawers, one piece) · 7 third profile-class miss ·
8 deferral avalanche (tone mismatch vs replica → low-conf drift at bulk scale;
24/31 deferred on DR133) · 9 white-on-white (OPEN, above).
Rubric changes bust the judge cache — batch them into one calibrated pass
against `output/.qa/labels.json` (frozen, 179 labels, NEVER modify).

## Operational notes

- Dev servers: `npm run dev` (backend :8000 --reload, frontend :5173).
- API key: read from `.env` directly (zshrc shadows it); app key in localStorage.
- Reliability monitor pattern: tail `output/.qa/reliability.jsonl`.
- Spend today: ~$25 total across ~135 images incl. all retries/experiments.
- Remaining bulk work queued: JR7/AR766/FC712/DR31 full palettes (~150 images;
  raised_panel class or global unlock needed for JR7/AR766).
- Shopify/Matrixify export: still blocked on product mapping from Jonathan.
